{% extends 'base.html.twig' %}

{% block body %}
    <h1>List of TV Show !</h1>

    <section class="series-list">
        {% for serie in series %}
            <article>
                <a href="{{ path('series_detail', {'id': serie.id }) }}">
                    <img src="{{ asset('img/posters/series/' ~ serie.poster) }}" alt="{{ serie.name }}">
                </a>
            </article>
        {% endfor %}
    </section>
{% endblock %}

{% block title %}
    {{ parent() }} | List
{% endblock %}

